#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first number to multiply
 * @b: the second number multiplying first number
 * Return: the result of multiplication between a param and b param
 */
int mul(int a, int b)
{
	return (a * b);
}
